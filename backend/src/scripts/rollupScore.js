// Placeholder — Roll Up Priority Score
// Aggregates child priority scores to the parent feature level.
export async function runRollupScore(pbClient, config, workspaceId) {
  return ['rollupScore: not yet implemented'];
}
